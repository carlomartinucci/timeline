<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @claim.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= @claim.body %>
</p>

<p>
  <strong>Person:</strong>
  <%= link_to @claim.person.name, @claim.person %>
</p>

<p>
  <strong>Claimed at:</strong>
  <%=l @claim.claimed_at rescue nil %>
</p>

<p>
  <strong>Happened at:</strong>
  <%=l @claim.happened_at rescue nil %>
</p>

<%= link_to 'Edit', edit_claim_path(@claim) %> |
<%= link_to 'Back', claims_path %>
